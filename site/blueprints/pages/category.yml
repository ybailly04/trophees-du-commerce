title: Catégorie

options:
  changeTemplate: false
  changeSlug: true

sections:
  candidates:
    type: pages
    label: Candidats de cette catégorie
    query: page.candidatesInCategory
    sortBy: count desc
    info: "{{ page.count }} votes"
    create: false
