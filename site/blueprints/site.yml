title: Site

columns:
    main:
      width: 2/3
      sections:
        pages:
          type: pages
        candidates:
          type: pages
          label: Candidats en attente
          layout: cards
          template: candidate
          query: site.find('candidates').drafts
    sidebar:
      width: 1/3
      sections:
        fields:
          type: fields
          fields:
            menus:
              type: structure
              fields:
                menuHeadline:
                  type: text
                  label: Nom du menu
                menu:
                  type: structure
                  fields:
                    link:
                      type: link
                      label: Lien
                    linkText:
                      type: text
                      label: Texte du lien
                    target:
                      type: toggle
                      text: Ouvrir dans une nouvelle fenêtre
            menuText:
              type: writer
              label: Texte du menu